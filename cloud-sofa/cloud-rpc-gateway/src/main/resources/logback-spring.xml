<?xml version="1.0" encoding="UTF-8"?>
<configuration scan="true" scanPeriod="60 seconds" debug="false">
    <springProperty scope="context" name="logging.level" source="logging.level.com.dcits"/>
    <springProperty scope="context" name="logging.level.root" source="logging.level.root"/>
    <property name="logging.path" value="./logs"/>
    <property name="spring.application.name" value="cloud-rpc-gateway"/>

    <!--控制台日志输出-->
    <appender name="STDOUT" class="ch.qos.logback.core.ConsoleAppender">
        <encoder class="ch.qos.logback.classic.encoder.PatternLayoutEncoder">
            <Pattern>%d{yyyy-MM-dd HH:mm:ss.SSS} [%X{SOFA-TraceId},%X{SOFA-SpanId}] [%thread] %-5level %logger{50} %line - %msg%n</Pattern>
        </encoder>
    </appender>

    <!--ERROR日志输出-->
    <appender name="ERROR-APPENDER" class="ch.qos.logback.core.rolling.RollingFileAppender">
        <append>true</append>
        <file>${logging.path}/${spring.application.name}/common-error.log</file>
        <filter class="ch.qos.logback.classic.filter.ThresholdFilter">
            <level>ERROR</level>
        </filter>
        <encoder class="ch.qos.logback.classic.encoder.PatternLayoutEncoder">
            <!--output format：%d is for date，%thread is for thread name，%-5level：loglevel with 5 character  %msg：log message，%n line breaker-->
            <pattern>%d{yyyy-MM-dd HH:mm:ss.SSS} [%X{SOFA-TraceId},%X{SOFA-SpanId}] [%thread] %-5level [%X{SOFA-TraceId},%X{SOFA-SpanId}]  %logger{50} - %msg%n</pattern>
            <!-- encoding -->
            <charset>UTF-8</charset>
        </encoder>
        <rollingPolicy class="ch.qos.logback.core.rolling.SizeAndTimeBasedRollingPolicy">
            <fileNamePattern>
                ${logging.path}/${spring.application.name}/%d{yyyyMMdd}/common-error.%d{yyyyMMdd}.%i.log.zip
            </fileNamePattern>
            <maxFileSize>50MB</maxFileSize>  <!-- 日志文件过大会使的编辑器打开非常慢，因此设置日志最大50MB -->
            <maxHistory>30</maxHistory>  <!-- 保存30天 -->
            <totalSizeCap>10GB</totalSizeCap>  <!-- 总日志大小 -->
        </rollingPolicy>
    </appender>



    <logger name="com.alipay.sofa" level="DEBUG" additivity="false">
        <appender-ref ref="ERROR-APPENDER"/>
        <appender-ref ref="STDOUT"/>
    </logger>

    <logger name="com.open.cloud" level="${logging.level}" additivity="false">
        <appender-ref ref="ERROR-APPENDER"/>
        <appender-ref ref="STDOUT"/>
    </logger>


    <root level="${logging.level.root}" additivity="false">
        <appender-ref ref="ERROR-APPENDER"/>
        <appender-ref ref="STDOUT"/>
    </root>

</configuration>