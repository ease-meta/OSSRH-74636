spring:
  application:
    name: cloud-eureka-service

server:
  port: 9525

eureka:
  client:
    healthcheck:
      enabled: true
    service-url:
      defaultZone: http://127.0.0.1:9527/eureka/
  instance:
    prefer-ip-address: true